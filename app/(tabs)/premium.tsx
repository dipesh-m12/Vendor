import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const pemium = () => {
  return (
    <SafeAreaView>
      <Text>pemium</Text>
    </SafeAreaView>
  );
};

export default pemium;

const styles = StyleSheet.create({});
